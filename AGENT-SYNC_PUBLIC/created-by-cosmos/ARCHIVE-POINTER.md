# Augment VSCode session exports — moved

This repository previously carried pre-Cosmos Augment VSCode conversation exports at
`AGENT-SYNC/archive/AugmentVSCode/session-exports/`, including raw `thread.json` logs.

**This repository is public, so those exports were publicly readable.** They have been removed and
consolidated into a private repository.

## Where they are now

```
mystarch_chief-of-staff/AGENT-SYNC/archive/AugmentVSCode/session-exports/resume/
```

That repository is private-forever with no public mirror. **This pointer intentionally 404s for
anyone without private access** — that is the access-control mechanism, not a broken link.

## Why

The exports are internal working conversations between Christopher Wilson and his agents. They were
scattered across eight repositories, five of them public. Consolidating them into one private
repository means a single place to protect rather than eight with differing publication rules.

## Need the historical context?

If you are working on this repository and need background from those threads, contact Christopher
([@drasticstatic](https://github.com/drasticstatic)). Summaries can be shared; raw threads are not
published.

---

*This repository is public, so it uses `AGENT-SYNC_PUBLIC/` and has no `logs/` directory.
`AGENT-SYNC/` was removed as part of the same change — it is the private-repo lane and should never
have existed here. Convention: [`my-template`](https://github.com/drasticstatic/my-template).*
